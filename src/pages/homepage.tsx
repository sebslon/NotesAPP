import { AddNote } from "components";
import { NotesList } from "components"

export const HomePage = () => {
    return (
        <>
            <AddNote />
            <NotesList />
        </>
    )
}
